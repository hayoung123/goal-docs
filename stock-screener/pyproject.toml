[project]
name = "stock-screener"
version = "0.1.0"
description = "Daily Korean stock screener with TradingView links"
requires-python = ">=3.11"
dependencies = [
  "finance-datareader>=0.9.96",
  "pandas>=2.2",
  "pyarrow>=15",
  "pyyaml>=6.0",
  "tabulate>=0.9",
]
